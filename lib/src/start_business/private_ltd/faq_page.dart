import 'package:flutter/material.dart';

class FAQPage extends StatelessWidget {

  List faqList = ["What documents are required to set up pvt.ltd Company in India","Do I have to have office space to start a company?","Who is Registrar of Company?"];
  List faqListAns = ["You need to arrange very simple documents of directors like photograph","No","Yes"];

  @override
  Widget build(BuildContext context) {
    return ListView.builder(itemBuilder: (BuildContext context, int index) { 
      //return ExpansionPanel(headerBuilder: null, body: null)
     },);
  }
}